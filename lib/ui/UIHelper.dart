import 'package:flutter/material.dart';

class UIHelper {
  static Padding padAll20 = Padding(padding: EdgeInsets.all(20));
  static Padding padAll10 = Padding(padding: EdgeInsets.all(10));

  static TextStyle loginTitleStyle = TextStyle(
    color: Color(0xFF282828),
    fontSize: 28,
    fontWeight: FontWeight.w600,
  );
}
